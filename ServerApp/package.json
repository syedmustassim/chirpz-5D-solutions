{
  "name": "serverapp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "jsonServer": "json-server --watch db.json --port 4000 --routes routes.json",
    "app": "json-server --watch db.json --port 4000 ",
    "start": "npx nodemon",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
